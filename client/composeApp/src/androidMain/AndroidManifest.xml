<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SingularApp"
        android:label="Singular"
        android:supportsRtl="true"
        android:theme="@style/Theme.Singular"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic is for talking to a local dev server over http://.
             Remove it before any real build: it disables Android's TLS enforcement app-wide. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
